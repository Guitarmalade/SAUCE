{
  "name": "@sauce/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('api tests not wired yet')\""
  },
  "dependencies": {
    "@sauce/curriculum": "workspace:*",
    "@sauce/domain": "workspace:*",
    "@sauce/validation": "workspace:*",
    "fastify": "^5.6.1"
  }
}

