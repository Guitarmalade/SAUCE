# Scripts

Use this directory for seeders, curriculum importers, maintenance tasks, and deployment helpers.

The `devtools/` subdirectory now holds the local Chrome launcher and the small CDP helper used by agents and manual debugging workflows.
